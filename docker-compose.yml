version: '3.1'

services:
  mysql:
    image: mysql:8.0
    container_name: mysql-container
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: billing  # 생성할 데이터베이스 이름
    ports:
      - "3305:3306"
