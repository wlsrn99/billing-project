spring:
  profiles:
    active: ci

---

spring:
  config:
    activate:
      on-profile: ci
  datasource:
    url: jdbc:mysql://localhost:3306/billing
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
